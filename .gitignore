node_modules/
.DS_Store
__pycache__/
*.pyc
.worktrees/

# API 키는 절대 저장소에 들어가면 안 된다. 확장은 압축만 풀면 소스가 보이므로
# 키가 커밋되는 순간 공개된 것과 같다 — 남의 사용량이 소유자에게 청구된다.
apikey.env.local
*.env.local
.env

# server-data/는 로컬 서버 런타임 데이터(사용량 및 암호화된 계정 정보)를 저장하므로 저장소에 들어가면 안 된다.
server-data/

# Chrome Web Store 배포 산출물 디렉터리
dist/

# 스킬 작업 산출물 — 루트의 _ 접두 이름은 Chrome 확장 로더가 예약어로 거부한다
_workspace/
.workspace/

# 로컬 영상 산출물
videos/
